services:
  seafile-mysql:
    image: mariadb:10.11
    container_name: seafile-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${SEAFILE_MYSQL_ROOT_PASSWORD}
      MYSQL_LOG_CONSOLE: "true"
      MARIADB_AUTO_UPGRADE: "1"
    volumes:
      - ./.volumes/seafile/mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -p$$MYSQL_ROOT_PASSWORD"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - dcu-net

  seafile-memcached:
    image: memcached:1.6.29
    container_name: seafile-memcached
    restart: unless-stopped
    entrypoint: memcached -m 256
    networks:
      - dcu-net

  seafile:
    image: seafileltd/seafile-mc:11.0-latest
    container_name: seafile
    restart: unless-stopped
    depends_on:
      seafile-mysql:
        condition: service_healthy
      seafile-memcached:
        condition: service_started
    environment:
      DB_HOST: seafile-mysql
      DB_PORT: "3306"
      DB_ROOT_PASSWD: ${SEAFILE_MYSQL_ROOT_PASSWORD}
      TIME_ZONE: UTC
      AUTHENTIK_URL: ${AUTHENTIK_URL}
      SEAFILE_ADMIN_EMAIL: ${SEAFILE_ADMIN_EMAIL}
      SEAFILE_ADMIN_PASSWORD: ${SEAFILE_ADMIN_PASSWORD}
      SEAFILE_SERVER_HOSTNAME: ${SEAFILE_DOMAIN}
      SEAFILE_SERVER_LETSENCRYPT: "false"
      SEAFILE_URL: ${SEAFILE_URL}
      SEAFILE_OIDC_CLIENT_ID: ${SEAFILE_OIDC_CLIENT_ID}
      SEAFILE_OIDC_CLIENT_SECRET: ${SEAFILE_OIDC_CLIENT_SECRET}
      FORCE_HTTPS_IN_CONF: "false"
    volumes:
      - ./.volumes/seafile/data:/shared
      - ./conf/seafile/seahub_settings.py:/shared/seafile/conf/seahub_settings.py:ro
    ports:
      - "127.0.0.1:50080:80"
    networks:
      - dcu-net

networks:
  dcu-net:
    external: true
